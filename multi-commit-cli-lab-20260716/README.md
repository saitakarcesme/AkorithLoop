# multi-commit-cli-lab-20260716

Checkpoint 2 parser foundation for a Node.js CLI lab that will later parse commit history and produce reports.

## Current Scope

- npm package metadata in `package.json`
- placeholder CLI entry point in `src/index.mjs`
- parser implementation in `src/args.mjs`
- parser tests for `--input`, `--format`, `--help`, unknown options, and missing values
- parser-only validation through `npm test`

## Usage

Run the placeholder CLI:

```sh
node src/index.mjs
```

Run the checkpoint validation:

```sh
npm test
```

## Checkpoint Status

- Checkpoint 1: complete — project skeleton and placeholder CLI.
- Checkpoint 2: complete — argument parser and parser-only tests.
- Checkpoint 3: pending — report generation.
- Checkpoint 4: pending — integration tests, expanded docs, and examples.

## Pending Work

- Report generation: pending for a later checkpoint.
- Integration tests: pending for a later checkpoint.
- Expanded docs and examples: pending for a later checkpoint.
